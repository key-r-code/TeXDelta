# TeXDelta Overleaf project

Replace the contents of `Old/` and `New/`, keeping a `main.tex` entry point in each folder, then recompile with pdfLaTeX. The bundled runner caches unchanged dependencies to avoid a latexmk loop.
